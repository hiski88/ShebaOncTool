# התחלה מהירה - פרוטוטייפ אונקולוגיה

## בדיקה מקומית ב-Windows

1. חלץ את תיקיית הפרויקט.
2. לחץ פעמיים על `RUN_LOCAL.cmd`.
3. בפעם הראשונה הסקריפט ייצור סביבת Python, יתקין את התלויות ויפתח את האפליקציה בדפדפן.
4. בדוק תחילה את שלושת הקבצים שבתיקיית `artifacts`.

## העלאת הקוד ל-GitHub

1. ודא שמותקן Git for Windows ושאתה מחובר לחשבון GitHub.
2. לחץ פעמיים על `PUBLISH_TO_GITHUB.cmd`.
3. הסקריפט יעדכן את המאגר `hiski88/ShebaOncTool` וימנע העלאה של קובצי לוז, נתוני עובדים וסודות.
4. בדוק שבכרטיסיית Actions ב-GitHub הבדיקה הסתיימה בירוק.

## חיבור אוטומטי ל-Streamlit

1. היכנס ל-Streamlit Community Cloud עם GitHub.
2. בחר Create app.
3. Repository: `hiski88/ShebaOncTool`.
4. Branch: `main`.
5. Main file path: `app.py`.
6. בחר Python 3.12.
7. לחץ Deploy.

מכאן כל הרצה נוספת של `PUBLISH_TO_GITHUB.cmd` תעדכן את GitHub, ו-Streamlit יפרוס את הקוד החדש אוטומטית.

## חיבור Google Calendar

בצע את השלבים המלאים בקובץ `DEPLOYMENT_HE.md`. בשלב הפרוטוטייפ השאר את אפליקציית OAuth במצב Testing והוסף את המשתמשים המורשים כ-Test users.

## בדיקות ראשונות מומלצות

1. כלי 1 - הזן עובד לדוגמה וסמן אי זמינות וחופש.
2. כלי 2 - הורד תבנית ריכוז, הדבק את הפלט והעלה אותה חזרה.
3. כלי 3 - העלה עותק XLSX של הלוז ובדוק שם מחוק, א"ת, מחליף, שישי ושבת.
4. אמת את שעות התורנויות בקובץ `config/oncology.json` לפני יצירת אירועים אמיתיים ביומן.

אין להעלות מידע רפואי מזהה. לפני שימוש מחלקתי אמיתי מומלץ להפוך את המאגר ואת האפליקציה לפרטיים או להוסיף מנגנון הרשאות.
